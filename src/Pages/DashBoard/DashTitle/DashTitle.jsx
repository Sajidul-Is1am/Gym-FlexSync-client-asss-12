/* eslint-disable react/prop-types */

const DashTitle = ({Title}) => {
    return (
        <div>
            <h2 className="text-6xl font-bold text-secondary">{ Title}</h2>
        </div>
    );
};

export default DashTitle;